import { fireEvent, render, screen, waitFor } from "@testing-library/react"; import { MemoryRouter } from "react-router-dom"; import { vi } from "vitest"; import "../i18n"; import Navbar from "../components/Navbar"; const logoutMock = vi.fn(); const navigateMock = vi.fn(); let currentUser = null; vi.mock("react-router-dom", async () => { const actual = await vi.importActual("react-router-dom"); return { ...actual, useNavigate: () => navigateMock }; }); vi.mock("../context/AuthContext", () => ({ useAuth: () => ({ user: currentUser, logout: logoutMock }) })); vi.mock("../api/townApi", () => ({ getAllTowns: vi.fn(() => Promise.resolve([ { id: 1, slug: "playas-del-coco", name: "Playas del Coco" }, { id: 2, slug: "tamarindo", name: "Tamarindo" }, { id: 3, slug: "samara", name: "Sámara" } ]) ) })); describe("Navbar", () => { beforeEach(() => { currentUser = null; logoutMock.mockClear(); navigateMock.mockClear(); localStorage.clear(); }); it("muestra enlaces principales cuando no hay usuario", async () => { render( <MemoryRouter>
<Navbar townSlug="playas-del-coco" />
</MemoryRouter> ); expect(screen.getByText(/Lugares/i)).toBeInTheDocument(); expect(screen.getByText(/Mapa/i)).toBeInTheDocument(); expect(screen.getByText(/Mi QR/i)).toBeInTheDocument(); expect(screen.getByText(/Empezar/i)).toBeInTheDocument(); await waitFor(() => { expect(screen.getByText(/Playas del Coco/i)).toBeInTheDocument(); }); }); it("muestra usuario y permite cerrar sesión", async () => { currentUser = { name: "Mario", email: "mario@gmail.com", role: "CLIENT" }; render( <MemoryRouter>
<Navbar townSlug="playas-del-coco" />
</MemoryRouter> ); expect(screen.getByText(/Mario/i)).toBeInTheDocument(); fireEvent.click(screen.getByText(/Salir/i)); expect(logoutMock).toHaveBeenCalled(); expect(navigateMock).toHaveBeenCalledWith("/p/playas-del-coco"); await waitFor(() => { expect(screen.getByText(/Playas del Coco/i)).toBeInTheDocument(); }); }); });